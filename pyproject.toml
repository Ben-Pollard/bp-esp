[project]
name = "bp-esp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pyserial>=3.5",
    "pytest-embedded[serial]>=2.9.3",
]
